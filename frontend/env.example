# Copy this file to .env and adjust if needed
# cp env.example .env

# Frontend API URL (what the browser uses)
# Use relative path for production (Vercel will proxy)
NEXT_PUBLIC_API_URL=/api

# Backend URL (for Next.js server-side rewrites)
# Local development:
BACKEND_URL=http://localhost:3002

# Production (uncomment for Vercel):
# BACKEND_URL=http://16.171.111.4:3002

